server:
  port: 8000
  servlet:
    context-path: /api/v1
spring:
  application:
    name: ecs-order
#  profiles:
#    active: test
management:
  endpoints:
    web:
      base-path: /manage

kafka:
  bootstrap:
    servers: kafka-1:19092,kafka-2:29092,kafka-3:39092
  group:
    id: group-1
